        <div class="navbar-nav user-menu">
            <% if current_user %>
                <span class="navbar-text current-user">
                    <span><%= current_user.email %></span>
                </span>
                <%# link_to "Logout", signout_path, method: :delete ,'class': "btn btn-light d-none d-lg-block" %>
                <%= button_to "logout", signout_path, data: {turbo_method: :delete} ,method: :delete, class: "btn btn-light d-none d-lg-block"  %>
            <% else %>
                <%= link_to "signup", signup_path, {class: "current-user" } %>
                <%= link_to "Login", signin_path, {class: "btn btn-light d-none d-lg-block"}%>
            <% end %>
        </div>
